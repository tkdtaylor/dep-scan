# Secrets
.env

# Python virtual environment (lives in the Docker volume — not part of the repo)
.venv/

# Build artifacts (language-dependent, will expand after ADR 001)
target/
.claude/worktrees/
.claude/sessions/
.claude/settings.local.json
codescan-reports/

# Python bytecode — also emitted by .claude/scripts/ hooks
__pycache__/
*.pyc

# Local-only retro notes, release evidence, and other operator-private scratch
archive/
